package aung.thiha.compose.navigation

/**
 * Right now, this doesn't do anything but if we want to provide extra info from a route in the future,
 * we can add functions here and override them in the concrete implementations
* */
interface Destination